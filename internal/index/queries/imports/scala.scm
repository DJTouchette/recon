; Scala import declarations. The whole declaration is captured; the dotted
; import prefix is reconstructed in Go (scala identifiers are flat children of
; import_declaration, optionally followed by selectors/wildcard).
(import_declaration) @path
