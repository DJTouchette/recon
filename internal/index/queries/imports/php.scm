; PHP use statements. @path captures the FQCN (App\Models\User), either as a
; backslash-qualified name or a single name.
(namespace_use_clause (qualified_name) @path)
(namespace_use_clause (name) @path)
