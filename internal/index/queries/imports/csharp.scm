; C# using directives. @path captures the dotted namespace. Covers qualified
; (A.B.C), single-identifier, and using-static forms.
(using_directive (qualified_name) @path)
(using_directive (identifier) @path)
